const letters=document.querySelectorAll('.hero h1 span,.hero h1 em');setInterval(()=>{letters.forEach(el=>{el.style.textShadow=`${Math.random()*4}px 0 22px currentColor`;setTimeout(()=>el.style.textShadow='0 0 25px currentColor',90)})},1800);
const cards=document.querySelectorAll('.card,.panel');cards.forEach(card=>card.addEventListener('mousemove',e=>{const r=card.getBoundingClientRect();card.style.background=`radial-gradient(circle at ${e.clientX-r.left}px ${e.clientY-r.top}px, rgba(255,59,213,.22), rgba(5,7,25,.85) 42%)`;}));cards.forEach(card=>card.addEventListener('mouseleave',()=>card.style.background='linear-gradient(145deg,rgba(13,6,42,.72),rgba(5,7,25,.8))'));
